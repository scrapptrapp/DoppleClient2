using System.Windows;
using Application = System.Windows.Application;

namespace DoppleWinClient
{
    public partial class App : Application
    {
        // This is where we'll put the "Restart if killed" logic later
    }
}
